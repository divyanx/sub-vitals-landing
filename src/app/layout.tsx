import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "SubVitals — Reddit CX Analytics for Brand Subreddits",
  description:
    "SubVitals gives moderators real-time CX analytics — auto-classification, sentiment tracking, crisis detection, and AI-powered triage — all natively inside Reddit.",
  openGraph: {
    title: "SubVitals — Reddit CX Analytics",
    description:
      "Real-time CX analytics for brand subreddits. Auto-classification, sentiment tracking, crisis detection.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-[#0a0a0a] text-white">
        {children}
      </body>
    </html>
  );
}
